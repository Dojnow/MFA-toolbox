
function int = detrint(db,de,dn)
int = round(logspace(log10(b),log10(e),n));

 % Function for setting of the detrending interval with logspace points.
 % db is the beginning of the interval; de is the end of the interval; dn is the number of poins in the inerval.
